#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main ()
{
	setlocale (LC_ALL, "Portuguese_Brazil");
	
	char nome[50];
	
	printf ("Digite seu nome: ");
	fgets (nome, 50, stdin);
	printf ("%s", nome);
	
	return 0;
}
